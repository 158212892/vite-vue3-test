import {bb} from "../getter-types"
const state = {
	bbb:888
}

const getters = {
	[bb]:state=>state.bbb
}

const mutations = {
	
}

const actions = {
	
}


export default {
	state,
	getters,
	mutations,
	actions
}
